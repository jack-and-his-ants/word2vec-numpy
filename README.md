# Word2Vec in NumPy

Implementation of the core training loop of the Word2Vec algorithm in pure NumPy.

Model:

* Skip-gram
* Negative Sampling

Goal of the project:
Implement forward pass, loss computation, gradient calculation, and parameter updates without using ML frameworks.
